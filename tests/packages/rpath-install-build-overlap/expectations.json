{
  "purpose": "An explicit install path wins over removal of the same build path.",
  "min_meson": "1.9",
  "platforms": [
    "linux",
    "darwin"
  ],
  "rules": [
    {
      "glob": "probe/_probe*",
      "paths": [
        "{origin}/keep"
      ]
    }
  ]
}
