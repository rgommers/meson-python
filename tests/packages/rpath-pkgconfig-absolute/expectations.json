{
  "purpose": "Preserve the absolute external RPATH supplied by pkg-config; use an absolute library filename like scipy-openblas64.",
  "min_meson": "0.64",
  "platforms": [
    "linux",
    "darwin"
  ],
  "rules": [
    {
      "glob": "probe/_probe*",
      "paths": [
        "{external}/lib"
      ]
    }
  ],
  "prepare": true
}
